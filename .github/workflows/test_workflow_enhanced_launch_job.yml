name: Test job enhanced launch job
run-name: ${{ github.actor }} is executing Test job enhanced launch job 🚀

on:
  workflow_dispatch:

jobs:
  test-workflow-job-enhanced-launch-job:
    uses: langtech-bsc/magma/.github/workflows/enhanced_launch_job.yml@16-file-injection
    secrets: inherit
    # with:
    #   runs_on: ubuntu-latest 

      
